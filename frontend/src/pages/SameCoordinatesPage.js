import React, { Component } from "react";

export default class SameCoordinatesPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <p>This is the Same coordinates page</p>;
  }
}
